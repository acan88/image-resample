I=imread('timg.jpg');%原始图像转化为灰度图像
subplot(2,2,1);imshow(I);title('原图');
f=rgb2gray(I);
subplot(2,2,2);imshow(f);title('灰色图像');
F=fft2(f);
F1=log(abs(F)+1);
subplot(2,2,3);imshow(F1,[]);title('傅里叶变换频谱图');
Fs=fftshift(F);
S=log(abs(Fs)+1);
subplot(2,2,4);imshow(S,[]);title('频移后的频谱图');
